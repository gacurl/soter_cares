<br>
<div class="container info-box">
  <h1 style="text-align: center; color: #0075b4;font-family: Source Sans Pro,Arial,Helvetica,sans-serif; font-size: 40px">Community Search</h1>
  <p style="font-family: Source Sans Pro,Arial,Helvetica,sans-serif">
    There are a total of <strong><%= @communities.count %> communities</strong> that are potential matches. <br>
    
    Next we need to know your Veteran's Administration status.
  </p>

<%= form_for(@prospect, url: update_veteran_journies_path(prospect_id: @prospect.id, lead_id: @lead.id)) do |f| %>
    <div class="row">
      <div class="col-md-6">
          <div class="form-group row">
            <div class="col-sm-12 col-sm-offset-4">
              <%= f.label :veteran, class: 'checkbox inline' do %>
              <%= f.check_box :veteran %>
                <span>Veteran or surviving widow of a veteran</span>
              <% end %>
            </div>
          </div>
        </div>
      </div>
          <%= f.submit "Next", class: "btn btn-primary" %>
<% end %>

</div>
<br>