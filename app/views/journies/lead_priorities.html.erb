<br>
<div class="container info-box">
  <h1 style="text-align: center; color: #0075b4;font-family: Source Sans Pro,Arial,Helvetica,sans-serif">Community Search</h1>
  <p style="font-family: Source Sans Pro,Arial,Helvetica,sans-serif">
    There are a total of <strong><%= @communities.count %> communities</strong> that are potential matches.<br>
   
    What is most important to you?
  </p>
  <div class="row">
    <div class="col-md-4">
      <%= link_to "Safety", update_lead_priorities_journies_path(prospect_id: @prospect.id, lead_id: @lead.id, priority: "Safety"), method: :post %><br>
      <%= link_to "Care", update_lead_priorities_journies_path(prospect_id: @prospect.id, lead_id: @lead.id, priority: "Care"), method: :post %><br>
      <%= link_to "Location", update_lead_priorities_journies_path(prospect_id: @prospect.id, lead_id: @lead.id, priority: "Location"), method: :post %><br>
      <%= link_to "Budget", update_lead_priorities_journies_path(prospect_id: @prospect.id, lead_id: @lead.id, priority: "Budget"), method: :post %><br>
      <%= form_tag(update_lead_priorities_journies_path) do %>
        <%= text_field_tag :priority, nil, placeholder: 'Enter a priority not listed', class: "form-control" %>
         <%= hidden_field_tag :prospect_id, @prospect.id %>
        <%= hidden_field_tag :lead_id, @lead.id %>
        <div><%= submit_tag 'Next', class: 'btn btn-primary' %></div>
      <% end %>
    </div>
  </div>
</div>
<br>